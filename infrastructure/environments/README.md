# Environments

Per-environment config notes (development / staging / production) —
which database each points to, which domains, which AzamPay mode
(sandbox vs live). Keep actual secrets out of this repo; reference where
they're stored (e.g. GitHub Actions secrets, host env vars).
