# Cloudflare

DNS, CDN, and cache-purge configuration for casuya.co.tz and
developer.casuya.co.tz. Actual API calls live in `integrations/cloudflare.py`
— this folder is for config/notes (zone settings, page rules, cache TTLs),
not code.
